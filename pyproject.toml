[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "relay-control"
version = "0.1.0"
description = "Deterministic reference control core for RELAY continuous blended assistance"
requires-python = ">=3.11"
license = {text = "Proprietary"}

[tool.setuptools.packages.find]
where = ["src"]
